<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mapper.UserMapper">
    <resultMap id="resultMap" type="src.com.po.User">
        <result property="password" column="pwd"/>
        <result property="id" column="id"/>
        <result property="username" column="username"/>
    </resultMap>

    <select id="getUserById" resultMap="resultMap" parameterType="Int">
        select * from mybatis.user where id=#{id}
    </select>

    <insert id="addUser" parameterMap="resultMap">
        insert into mybatis.user(id,username,pwd) values(#{id},#{username},#{pwd})
    </insert>

    <delete id="deleteUser" parameterType="int">
    delete from mybatis.user where id=#{id}
    </delete>

</mapper>